# evatt: decisions and open items

Written 10 September 2026, at the end of the implementation of
`docs/superpowers/plans/2026-09-09-evatt.md`. The working ledger it came from was
scratch and has been deleted; git history is the record of the code, and this file
is the record of the judgement calls made along the way.

Every entry below is a decision taken without asking, on the reasoning stated, with
the cost of being wrong. Rework anything here that reads wrong.

## Rulings (41)

1. this ledger's first line uses a hyphen, not the em dash the skill template shows. Reason: the repository prose guard rejects em dashes in written files, and the recovery contract only requires the first line to name the plan file, which a hyphen preserves. Cost if wrong: a future controller matching the template literally could fail to recognise the ledger and re-dispatch completed tasks, so the plan path is spelled out in full on that line.
2. `verify.findings` runs the residual sweep over the redacted form rather than the raw text. Reason: running it raw reported a known client twice, once as a mapped entity and again as an unclassified name, which buries the genuinely unknown items the operator must act on. Cost if wrong: verify under-reports a name that is in the map but which the operator wanted re-surfaced, caught by Task 6's verify tests.
3. Task 3's `test_assign_numbers_within_a_kind` now asserts `PERSON_02` exactly, replacing `PERSON_01 or PERSON_02`. Reason: an assertion satisfied by two different behaviours pins neither, and the review rubric treats it as a test that asserts nothing. Cost if wrong: the test is over-tight and fails on a legitimate change to ordinal allocation, which is a visible failure rather than a silent one.
4. Halt.unknowns was annotated tuple[object, ...] by the brief, and tuple covariance makes that unassignable to the tuple[Unknown, ...] parameter Task 8 declares. Decided to fix it in Task 1 rather than Task 8 because the field lives in a Task 1 file. Cost if wrong: a TYPE_CHECKING forward reference to a module that does not exist yet, which resolves to Any until Task 4 lands redact.py, so the fix is inert rather than harmful in the meantime.
5. pulled two Minor findings into the fix round rather than deferring them, because both sit in files the Important fix already touched and cost no extra round trip. Cost if wrong: a slightly larger fix diff than the loop rules intend.
6. the first python -m build moved from Task 8 step 5 to Task 9 step 5. Reason: pyproject declares readme = README.md and Task 9 creates that file, so a build in Task 8 fails. Found by the Task 1 implementer, not by my pre-flight scan. Cost if wrong: the packaging break surfaces one task later than it could have.
7. a labelled identifier is admitted on the label alone, without a check digit. Reason: the reviewer read the origin module and found the brief had REVERSED its deliberate decision, whose comment says the labelled digits are strong enough evidence on their own. A labelled TFN failing the check digit was dropped entirely, and nothing downstream would catch it: the residual sweep looks only at names, addresses and dates, so verify reported clean. Cost if wrong: a number following the word TFN that is not one gets a placeholder, which is over-redaction and visible in the manifest.
8. extended the same treatment to labelled ABN, ACN and Medicare, which had no labelled patterns at all. Reason: same leak, same invisibility. Cost if wrong: three more patterns to maintain.
9. removed the twelve month names from the statutory word list. Reason: they suppressed real people. June Smith, April Jones, August Meyer and Ray May were all missed by the redaction pass, missed by the halt sweep, and verify reported clean. June, April, May and August are common Australian given names and May, Court and Board are surnames. Cost if wrong: accounting terms like "June Quarter" now halt for triage, raising the halt rate on real workpapers. That is the safe direction and what the triage loop is for.
10. all four bare patterns carry the one-character money guard only. I first told the implementer to align them upward to TFN_BARE's two-character guard, which deleted real detections in table rows ("row 7 123456780", "in 2019 2123456701 was issued"). Reverting that left the most sensitive identifier with the weakest detection, so round 3 dropped the guard from TFN_BARE too. Cost if wrong: a grouped money amount whose nine-digit tail satisfies the mod-11 check is redacted as a TFN, at roughly one such tail in eleven. Accepted because over-redaction costs a placeholder and under-detection leaks a tax file number.
11. stopped after three fix rounds rather than continuing to tune patterns. Reason: each round surfaced further edge cases, which is the "your own fixes become the main defect source" pattern. Round 3 was verified by a 447-string differential showing zero lost detections. Cost if wrong: the two deferred leak-shaped items below stay open until someone decides on them.
12. deleted the hand-rolled gitignore text scan and asked git instead, via local "git check-ignore --quiet" and "git ls-files --error-unmatch". Reason: the brief specified a structural scan, and the review found six ways it was wrong. It failed open on a negation rule, on a .gitignore outside the repository, and on an already-tracked file, which .gitignore does not affect at all. That last case is the worst, because it is exactly the state where the key is already in history and the guard is reassuring the caller. It also refused valid rules git honours and never read .git/info/exclude. The replacement deleted four net lines. Cost if wrong: the guard now needs git on PATH and a work tree, and it fails closed without them.
13. save now calls require_gitignored on its temporary file, so the temporary is held to the map's own standard. Reason: save wrote entities.json.tmp, which no ignore rule covered, so a hard kill left a complete plaintext copy of the key at a committable path. Consequence accepted honestly by the implementer rather than papered over: save now requires a git work tree, and two save tests moved into throwaway repositories. This is consistent, because the Task 8 CLI already refuses a map outside a repository. Cost if wrong: a map kept outside any repository cannot be saved, which is a deliberate refusal rather than a silent failure.
14. assign uses monotonic max+1 per kind rather than filling gaps. Reason: reissuing an ordinal freed by a deletion gives a new entity a placeholder that already means someone else, and restore then puts the wrong real name into an answer. Cost if wrong: ordinals grow monotonically and are never compacted.
15. closed the task on my own direct verification rather than a third review dispatch. The diff was four small items, the implementer mutation-tested three and honestly reported the fourth as unkillable in-process, and I ran the suite, the linters and the ignore check myself. Cost if wrong: a small unreviewed fix diff, mitigated by the final whole-branch review.
16. reviewed Tasks 4 and 5 together rather than separately. Task 4 left residual as a stub that Task 5 replaced in the same file, so reviewing the stub would have been waste. Cost if wrong: a defect in passes one and two survived one extra task before review. Task 5 did not touch those passes.
17. fixed two leaks whose root cause sits in patterns.py, which Task 2 had closed. Reason: fix the bug where it is, not where it surfaces. One statutory word absorbed the person beside it, because NAME greedily takes a third token and is_statutory rejected the whole candidate. "Priya Sharma Superannuation" is how an SMSF is named, and "the Board Daniel Okafor" is ordinary workpaper prose; both returned unchanged with an empty manifest and no halt. Cost if wrong: Task 2's 33 tests could have regressed. They did not; all still pass unmodified.
18. the residual sweep scans the whole text once and maps offsets back to line numbers, rather than scanning per line. Reason: NAME's \s+ matches a newline, so a name broken by a hard wrap was invisible to a per-line scan, and hard-wrapped markdown is the normal case. Cost if wrong: the line and context contract had to be preserved through an offset map, which was verified against a naive line walk at every offset of six texts with zero mismatches.
19. the first attempt at that whole-text scan reintroduced the same silent-pass class across line breaks, because a rejected candidate consumed tokens finditer never re-offered. A markdown heading ending in a statutory word merged with the name below it. A randomised sweep put the silent-leak rate at 54.7 per cent. Fixed by resuming the scan at the rejected candidate's second token rather than past it, which took the sweep to 0.0 per cent and also fixed the pre-existing single-line analogue. Cost if wrong: an explicit search loop replaces finditer, so a non-terminating loop was the risk; pos strictly increases and the ReDoS timing tests still pass.
20. widened the name and address token classes to Latin-1 accented letters. Reason: Zoe Nguyen halted but Zoe-with-diaeresis did not, and accented names are ordinary in Australian client data. Deliberately not extended to Cyrillic or CJK. Cost if wrong: more over-detection, which reaches triage.
21. accepted the implementer's judgement call adding "Appeals" to the statutory word list, so Administrative Appeals Tribunal stays filtered whole after the window retry. Cost if wrong: a person surnamed Appeals is missed. There is no such Australian surname.
22. closed on my own direct verification rather than a third review dispatch. I ran the suite and all twelve leak and statutory vectors myself. Cost if wrong: a two-item fix diff goes to the final whole-branch review unreviewed, after being mutation-tested and swept over 600 documents.
23. restore uses a symmetric placeholder boundary, and the boundary itself moved into a single shared constant PLACEHOLDER_BOUNDARY in patterns.py. Reason: restore was the only component that reimplemented the placeholder match, and it dropped the left boundary the others carry, so "PRIOR_CLIENT_01" passed redaction with no halt, passed verify in front of a partner, and then restore wrote the real client name into a position it never occupied. Choosing the symmetric form over the minimum fix also leaves "CLIENT_01s" unrestored rather than silently writing "Sample Holdings Pty Ltds". Cost if wrong: a placeholder followed by a word character is left unrestored, which is visible in the answer rather than silently wrong.
24. accepted the implementer's third guard clause beyond the two I specified, skipping an entity whose VALUE contains a placeholder. It is the other half of the guard _replace_entities already applies and the only thing that closes the cascade where one hand-built entity's value carries another's placeholder.
25. endorsed _carried_placeholders as designed and accepted its residual limit. Reporting every placeholder is not viable, because a correctly redacted file is nothing but placeholders and verify must return clean on one or nobody will trust it. A lone assigned CLIENT_01 in a document with no other trace is character for character identical to correct output given the text and the map; the reviewer confirmed no cheap check closes it, because the manifest, a round trip and ordinal density all read identically in both cases. redact's strict halt stays the place that case is caught. Cost if wrong: a file this pipeline never redacted, carrying a literal assigned placeholder, verifies clean.
26. reviewed once rather than twice, verifying the fix round myself. Cost if wrong: a mutation-tested two-item diff reaches the final review unreviewed.
27. dispatched Tasks 7 and 8 as one unit and reviewed them together, because Task 8's tests consume Task 7's fixtures. Cost if wrong: a larger diff for one reviewer.
28. the CLI normalises CRLF to LF for detection and restores the file's original ending on write, rather than widening the digit separators in seven patterns. The implementer's newline-preserving fix for byte-exactness had created a worse bug than it cured: every digit-run pattern separates groups with a single whitespace character, and a CRLF pair is two, so "TFN: 123\r\n456 782" was not detected at all. A live tax file number reached the output file with no halt and verify reported clean, on Windows, the platform this box runs. Cost if wrong: a mixed-ending file is normalised to its dominant ending rather than preserved.
29. moved that normalisation out of the CLI and into redact and verify themselves. I found by probing the library API that the CLI was safe while a direct call to redact() was not, and redact is public in a py.typed package. Fix it once where all callers route through. The implementer confirmed verify needed it separately, because findings runs structured_spans over the raw text as well, and verified restore did not need it rather than assuming. Cost if wrong: byte-exactness is now a CLI-layer property rather than a redact property, which is correct layering but had to be documented.
30. --out refuses to equal the resolved map or input, including the manifest and triage paths it derives. Reason: "redact --map entities.json --out entities.json" exited 0 and overwrote the key with redacted markdown, making every previously redacted document unrestorable, from one typo. Cost if wrong: a legitimate in-place workflow is refused, which nobody wants anyway.
31. the triage file's carried-placeholder context now comes from the redacted text. Reason: _input_placeholders ran on the raw input, so one triage file held both forms of a line and a full TFN, an email and a mapped client name landed in plaintext in the directory the operator was about to send from. Also added *.triage.md to the package gitignore.
32. a halt now deletes a pre-existing output and manifest at that path. Reason: the halt said "nothing was written" while a previous run's output sat beside the new triage file, and the stale file is what an operator sends.
33. identifiers.md uses a checksum-invalid labelled ABN and an ACMA fictitious-range phone number instead of the ATO's own real ABN. Reason: labelled identifiers are admitted on the label alone since Task 2, so the sample still covers all seven kinds without shipping a real active entity's number as package data. The documented test vectors stay in tests/test_patterns.py where they belong.
34. dispatched and will review Tasks 9 and 10 as one unit. They are coupled, since Task 10's CI runs the build Task 9 enables and its path filters name Task 9's documents.
35. accepted the implementer strengthening the retired-claims list beyond the brief, to substrings (anonymis, anonymiz, anonymity, not personal information, cannot be re-identified, guarantee, compliant with the privacy act) and to cover CONTRIBUTING and SECURITY as well as the three documents I named. The brief's list would have caught "anonymised" and "anonymous" but not "anonymisation", which is the wording most likely to appear. Cost if wrong: a legitimate future sentence containing one of those substrings is blocked, which is a visible test failure.
36. accepted its addition of a bullet to DATA-FLOW naming that every command spawns git. The brief's zero-network text claimed all processing is in memory and locally without qualification, and an unqualified claim there is the kind an auditor pulls on.
37. accepted its rewrite of the CI demo's halt assertion. The brief's "test ! -f build/halt.md" passes trivially when nothing was ever written there, so it did not test the stale-output deletion at all. The demo now writes a real output to that path first.
38. fixed the case-insensitivity Critical rather than shipping it. A mapped entity written in lower case was replaced by neither pass and reported by neither, because pass two matched case-sensitively and the residual sweep requires a capital first letter. "client: sample holdings pty ltd" is ordinary markdown front matter, and redact exited 0 with an empty manifest while verify said clean. Cost if wrong: a short common-word entity value now over-replaces in ordinary prose, which is the safe direction.
39. did not invent an ATO client reference detector, although the spec named one. There is no single published fixed format, so a pattern would be guesswork producing noise or false confidence. Recorded it in the README as a named limit instead: evatt does not detect ATO client references, and a document carrying one relies on the human check. Cost if wrong: a real identifier class goes undetected, now disclosed rather than silent.
40. restore --out stays unguarded by require_gitignored, unlike the map, its temporary and the triage file. A guard on an arbitrary operator-chosen answer path would push operators towards a broad ignore rule that weakens the guards that matter. Documented in the CLI docstring, README and DATA-FLOW. Cost if wrong: a restored answer holding real client names can be committed.
41. fixed the regression the fix wave introduced rather than surfacing it. value_pattern became case-insensitive while the two guards testing whether a VALUE is placeholder-shaped stayed case-sensitive, so an entity valued tfn_01 destroyed the TFN_01 placeholder pass one had just written and made the manifest count a client that never appeared. A two-line regression introduced by this session is incomplete work, not a residual finding to hand over. Cost if wrong: one more round than the process nominally allows.

## Deferred minors, shipped as they are (28)

- RED evidence was a single collection error, so the gitignore assertion was never observed failing on its own condition before implementation.
- the Halt message ends "nothing was written" and Task 8's CLI prints the same sentence separately; two copies that can drift. Pointer carried into the Task 8 dispatch.
- no MANIFEST.in, unlike the sibling package, so samples reaching the sdist relies on implicit setuptools inclusion. Pointer carried into the Task 9 dispatch, which should inspect the sdist and not only the wheel.
- once Unknown is real, mypy over tests/ would reject Halt(("Jane Roe",)). The binding config is packages = ["evatt"] and the documented command is mypy evatt, so this never fires today.
- ignore_missing_imports = true makes the forward reference fail soft; if redact.py later names the class something else, mypy falls back to Any rather than erroring.
- the gitignore test's .strip() is left-lenient, so "  entities.json" passes though git treats leading spaces as part of the pattern.
- interior partial spans leave a residue, "ref 70 669 074 391 end" reports only the ACN tail and leaves "70 " beside the placeholder.
- NAME over-matches leading words, so triage values can read "Dear Jane Roe" or "Ms Jane Roe". Ruled: leave the origin regex alone until real halt-rate data exists.
- BSB fires on hyphenated GL account codes such as 410-100. Document in the Task 9 README.
- non-ASCII decimal digits pass the gate. Over-detection, inherited from the origin.
- a changed test asserts valid_acn("123456780") twice, and its docstring still names a vector replaced two rounds back.
- no fsync of the containing directory before os.replace. File-level fsync is in place; the directory case is a portability rabbit hole.
- the three old comment-handling guard tests have no named successor, because comment handling is now git's job. Behaviour confirmed still correct by probe.
- the git-absent skip guard could mask the nine real-repository guard tests on a runner without git. Task 10 puts evatt on ubuntu-latest, where git is always present.
- load accepts "X" and " X " as distinct values, so one real name could take two placeholders.
- redact(strict=False) discards the input-placeholder report. CARRIED INTO TASK 6, which builds verify on strict=False and would otherwise miss a placeholder already present in the input.
- _replace_entities silently skips an empty or placeholder-shaped value rather than raising. Needs a hand-built Entity; load blocks the normal path.
- residual returns all addresses, then dates, then names, rather than in line order. No consumer yet; the Task 8 triage file may want line order.
- names dedupe per line while addresses report per occurrence, so the Halt count understates names.
- "Schedule Two\n<Given>" reports the partial window "Two\nGiven" and still halts. Triage noise, pre-existing, not a leak.
- both ABN_10 and ABN_100 can be minted in one document past 105 identifiers of a kind.
- Unknown.context is an unbounded whole line, so a wide table row carries a lot of raw client text into the triage file.
- verify imports the private _PREFIX from entities. Justified in a comment as drift-proofing.
- an entity whose value is itself a structured identifier is reported twice by verify, once as the identifier kind and once as the entity kind. Over-detection.
- verify prints the real values it found to the terminal, so a TFN can land in a shell recording or CI log. To be noted in the Task 9 README.
- the strict= source tripwire in test_cli.py is defeatable by spacing.
- --out naming a directory raises rather than exiting 1.
- Unknown.context is still an unbounded whole line in the triage file, now always post-redaction.

## Carried forward and not verified

- Task 3: carry into Task 8: nothing calls require_gitignored yet. The CLI must call it before the first save, and before redact reads the map.
- Task 3: carry into Task 8: the Halt message ends "nothing was written" and the planned CLI prints the same sentence separately.
- Task 6: carry into Task 8: the CLI must NEVER expose a non-strict redact. The residual placeholder limit's mitigation depends on it, and the verify docstring now says so.
- Task 6: carry into Task 8: the brief's step 4 test counts are stale throughout; treat them as indicative, not as a gate.
- Task 7/8: not verified: wheel and sdist contents, and Python 3.10. The suite ran on 3.12 and the first build is Task 9.
- Task 9/10: open for the final review: evatt's sdist carries only README.md and LICENSE, where the sibling's MANIFEST.in also ships SECURITY.md. Divergence from house form, not a defect.
- Task 9/10: open for the final review: cli.py's _collision docstring claims "--out entities" beside a map at "entities.json" collides through the derived manifest path. It does not; _manifest_path yields "entities.manifest.json". The guard is real, the docstring overstates it. One-line correction.
- FINAL: not verified: the GitHub workflow has never run; the 3.10 CI leg fetches a 3.12 interpreter for the build step via uv automatic downloads, which is untested. The clean-wheel demo was run locally by extracting the YAML run block, not by CI.
